export default function BookingsPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md flex-col p-6">
      <h1 className="text-2xl font-bold">Your bookings</h1>
      <p className="mt-2 text-zinc-600">
        Confirmed bookings, live status, and rate-on-completion will appear here.
      </p>
      <div className="mt-12 rounded-2xl border border-dashed border-zinc-300 p-8 text-center text-zinc-500">
        Wired up in implementation steps 9–11 (booking flow + Billplz + check-in).
      </div>
    </main>
  );
}
