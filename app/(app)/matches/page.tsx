export default function MatchesPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md flex-col p-6">
      <h1 className="text-2xl font-bold">Your matches</h1>
      <p className="mt-2 text-zinc-600">
        Once a helper accepts your interest, they'll appear here. Tap to chat and book.
      </p>
      <div className="mt-12 rounded-2xl border border-dashed border-zinc-300 p-8 text-center text-zinc-500">
        Wired up in implementation step 8 (match detail + chat). Stub for now.
      </div>
    </main>
  );
}
